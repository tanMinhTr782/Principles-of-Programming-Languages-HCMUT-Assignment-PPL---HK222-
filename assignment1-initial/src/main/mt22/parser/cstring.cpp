#include <cstring>
#include <iostream> 
void process (const char str[], char outstr[]){
char* strres = new char(1000);
     int size = strlen(str); 
     for (int i = 0; i < size; ++i){
        
     }
}
int main (){
}